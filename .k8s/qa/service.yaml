apiVersion: v1
kind: Service

metadata:
  name: doctor-patient-ai-transcript
  namespace: qa

spec:
  type: LoadBalancer

  selector:
    app: doctor-patient-ai-transcript

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000